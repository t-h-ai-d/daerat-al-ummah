# Community post menu verification

The communities index was visually checked at desktop width after the community-post parity update. The Arabic RTL layout, empty-state card, navigation shell, and creation call-to-action remain visible at normal zoom.

The checked `/communities/test` route correctly remained in its loading state because there is no test community record in the current database. The author menu and report dialog require a real community post and will be verified after the Render deployment has the repair release and a member-created post is available.
