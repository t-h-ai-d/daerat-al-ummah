# حالة إصلاح الرفع الحي

تم التحقق في 18 أغسطس 2026 من أن خدمة Render كانت تعرض الإصدار السابق: طلب `PUT /api/uploads/relay` أعاد صفحة HTML بحالة `200` بدل استجابة JSON. ظهر في لوحة Render بعد ذلك نشر تلقائي ناجح للالتزام `2505874` عند 20:51:18 بتوقيت GMT+1؛ لذلك يجب أن يرد المسار الآن بـ JSON حتى بلا جلسة، وسيُعاد اختباره مباشرةً.
