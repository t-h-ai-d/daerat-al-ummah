import EmojiPicker from "@/components/EmojiPicker";
import PostComments from "@/components/PostComments";
import PlatformShell from "@/components/PlatformShell";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { useAuth } from "@/_core/hooks/useAuth";
import { ownerReviewMailto } from "@/lib/ownerReview";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Eye, FileText, Flag, Globe2, ImagePlus, Layers3, Link2, Loader2, LockKeyhole, MessageSquareText, MoreHorizontal, Pencil, Plus, Trash2, Upload, UsersRound, Video } from "lucide-react";
import { useRef, useState } from "react";
import { toast } from "sonner";
import { useLocation, useParams } from "wouter";

type CommunityKind = "community" | "group";
type Visibility = "public" | "members";
type Attachment = { id?: number; kind: "image" | "gif" | "video" | "file" | "link"; url: string; storageKey?: string | null; filename?: string | null; mimeType?: string | null; sizeBytes?: number | null };
type CommunityPost = { id: number; title?: string | null; content: string; visibility: "public" | "friends"; createdAt: Date; author: { id: number; name: string | null; username: string | null }; attachments: Attachment[]; likeCount: number; commentCount: number; repostCount: number };
type ReportCategory = "scam" | "lie" | "brainrot" | "haram imagery";

function communitySlugFromText(value: string) {
  return value.trim().replace(/\s+/g, "-").replace(/[^a-zA-Z0-9\u0600-\u06FF\u0750-\u077F\u08A0-\u08FF-]/g, "").slice(0, 96);
}

function visibilityLabel(visibility: Visibility) {
  return visibility === "public" ? "عام" : "للأعضاء";
}

function kindLabel(kind: "community" | "group" | "subcommunity") {
  if (kind === "group") return "مجموعة";
  if (kind === "subcommunity") return "مجتمع فرعي";
  return "مجتمع";
}

function CommunityBadge({ kind, visibility }: { kind: "community" | "group" | "subcommunity"; visibility: Visibility }) {
  return <div className="flex flex-wrap items-center gap-2 text-[11px] font-extrabold">
    <span className="rounded-full bg-[#e6f0e5] px-2.5 py-1 text-[#176047]">{kindLabel(kind)}</span>
    <span className="flex items-center gap-1 rounded-full bg-[#f6f0df] px-2.5 py-1 text-[#896c1f]">{visibility === "public" ? <Globe2 size={12} /> : <LockKeyhole size={12} />}{visibilityLabel(visibility)}</span>
  </div>;
}

function AttachmentPreview({ attachment }: { attachment: Attachment }) {
  if (attachment.kind === "image" || attachment.kind === "gif") return <img src={attachment.url} alt={attachment.filename || "مرفق منشور"} className={`mt-4 max-h-96 w-full rounded-xl border border-[#e0e8de] ${attachment.kind === "gif" ? "object-contain bg-[#f7faf6]" : "object-cover"}`} />;
  if (attachment.kind === "video") return <video controls preload="metadata" className="mt-4 max-h-96 w-full rounded-xl border border-[#e0e8de] bg-[#183b32]"><source src={attachment.url} type={attachment.mimeType || "video/mp4"} />لا يدعم متصفحك تشغيل هذا الفيديو.</video>;
  return <a href={attachment.url} target="_blank" rel="noreferrer" className="mt-4 flex items-center gap-3 rounded-xl border border-[#e0e8de] bg-[#fafcf9] p-3 transition-colors hover:border-[#b9d0bd]"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#e1ebdf] text-[#2a6651]">{attachment.kind === "file" ? <FileText size={17} /> : <Link2 size={17} />}</span><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold text-[#24483d]">{attachment.filename || (attachment.kind === "file" ? "ملف مرفق" : attachment.url.replace(/^https?:\/\//, ""))}</p><p className="mt-0.5 text-[11px] text-[#809488]">افتح المرفق في علامة تبويب جديدة</p></div><ArrowUpRight size={16} className="text-[#5b8875]" /></a>;
}

function CommunityPostCard({ post, isAuthor, onReport, onDelete, onEdit, onSetVisibility }: { post: CommunityPost; isAuthor: boolean; onReport: () => void; onDelete: () => void; onEdit: () => void; onSetVisibility: (visibility: "public" | "friends") => void }) {
  const authorName = post.author.name || post.author.username || "عضو في الدائرة";
  return <article className="rounded-[24px] border border-[#dce5da] bg-white p-5 shadow-[0_12px_30px_rgba(16,61,46,0.04)]">
    <div className="flex items-start justify-between gap-3">
      <div className="min-w-0"><p className="text-sm font-extrabold text-[#244c3e]">{authorName}</p><p className="mt-0.5 text-[11px] text-[#81978b]">{new Date(post.createdAt).toLocaleString("ar")}</p></div>
      <div className="flex items-center gap-2"><span className="rounded-full bg-[#e6f0e5] px-2.5 py-1 text-[11px] font-bold text-[#176047]">{post.visibility === "public" ? "عام" : "للأصدقاء"}</span><DropdownMenu><DropdownMenuTrigger asChild><button className="grid h-8 w-8 place-items-center rounded-lg text-[#698478] transition-colors hover:bg-[#edf4ee] hover:text-[#176047]" aria-label="خيارات المنشور" title="خيارات المنشور"><MoreHorizontal size={18} /></button></DropdownMenuTrigger><DropdownMenuContent align="end" className="min-w-48 border-[#dbe5dc] bg-[#fbfcf8] text-[#284b3e]">{isAuthor ? <><DropdownMenuLabel>خيارات منشورك</DropdownMenuLabel><DropdownMenuItem onSelect={onEdit}><Pencil />تحرير المنشور</DropdownMenuItem><DropdownMenuItem onSelect={() => onSetVisibility("public")} disabled={post.visibility === "public"}><Eye />إظهاره للعامة</DropdownMenuItem><DropdownMenuItem onSelect={() => onSetVisibility("friends")} disabled={post.visibility === "friends"}><UsersRound />للأصدقاء فقط</DropdownMenuItem><DropdownMenuSeparator /><DropdownMenuItem variant="destructive" onSelect={onDelete}><Trash2 />حذف المنشور</DropdownMenuItem></> : <><DropdownMenuLabel>خيارات المنشور</DropdownMenuLabel><DropdownMenuItem onSelect={onReport}><Flag />الإبلاغ عن المنشور</DropdownMenuItem></>}</DropdownMenuContent></DropdownMenu></div>
    </div>
    {post.title && <h3 className="mt-4 font-display text-lg font-semibold text-[#244c3e]">{post.title}</h3>}
    <p dir="auto" className="mt-3 whitespace-pre-wrap text-sm leading-7 text-[#3e5c50]">{post.content}</p>
    {post.attachments.map((attachment, index) => <AttachmentPreview key={attachment.id ?? `${attachment.url}-${index}`} attachment={attachment} />)}
    <div className="mt-4 flex items-center justify-between gap-2 border-t border-[#edf2eb] pt-3"><PostComments postId={post.id} commentCount={post.commentCount} /><p className="text-xs text-[#84978c]">{post.likeCount} إعجاب · {post.repostCount} مشاركة</p></div>
  </article>;
}

function CreateCommunityForm({ parentId, onDone }: { parentId?: number; onDone: (slug: string) => void }) {
  const utils = trpc.useUtils();
  const [name, setName] = useState("");
  const [slug, setSlug] = useState("");
  const [slugTouched, setSlugTouched] = useState(false);
  const [description, setDescription] = useState("");
  const [kind, setKind] = useState<CommunityKind>("community");
  const [visibility, setVisibility] = useState<Visibility>("public");
  const create = trpc.social.createCommunity.useMutation({ onSuccess: (_, input) => { toast.success(parentId ? "تم إنشاء المجتمع الفرعي." : "تم إنشاء المساحة."); void utils.social.communities.invalidate(); onDone(input.slug); }, onError: error => toast.error(error.message) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, slug: communitySlugFromText(slug || name), description, kind: parentId ? "group" : kind, parentId, visibility }); };
  return <form onSubmit={submit} className="space-y-3 rounded-[24px] border border-[#d9e4d7] bg-white p-5 shadow-[0_14px_38px_rgba(16,61,46,0.06)]"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#e5efe5] text-[#176047]"><Plus size={18} /></span><div><h2 className="font-display text-lg font-semibold text-[#214b3c]">{parentId ? "أنشئ مجتمعًا فرعيًا" : "أنشئ مساحة نافعة"}</h2><p className="mt-0.5 text-xs leading-5 text-[#788f84]">{parentId ? "تابع موضوعًا محددًا داخل هذا المجتمع." : "يمكن أن تكون مجتمعًا واسعًا أو مجموعة صغيرة واضحة الهدف."}</p></div></div><Input required value={name} onChange={event => { const nextName = event.target.value; setName(nextName); if (!slugTouched) setSlug(communitySlugFromText(nextName)); }} maxLength={120} placeholder="اسم المساحة" className="h-11 rounded-xl border-[#dce5da] text-right" /><Input value={slug} onChange={event => { setSlugTouched(true); setSlug(communitySlugFromText(event.target.value)); }} maxLength={96} placeholder="رابط قصير اختياري — يُنشأ تلقائيًا من الاسم" dir="auto" className="h-11 rounded-xl border-[#dce5da] text-right" /><p className="-mt-1 text-[11px] leading-5 text-[#788f84]">يمكنك استعمال العربية أو الإنجليزية في الرابط، أو تركه كما تولّد تلقائيًا.</p><Textarea required value={description} onChange={event => setDescription(event.target.value)} minLength={12} maxLength={1600} placeholder="ما الفائدة التي ستقدّمها هذه المساحة؟" className="min-h-24 rounded-xl border-[#dce5da] text-right" /><div className="grid gap-3 sm:grid-cols-2">{!parentId && <label className="space-y-1 text-xs font-bold text-[#47685b]"><span>النوع</span><select value={kind} onChange={event => setKind(event.target.value as CommunityKind)} className="h-11 w-full rounded-xl border border-[#dce5da] bg-white px-3 text-right"><option value="community">مجتمع</option><option value="group">مجموعة</option></select></label>}<label className="space-y-1 text-xs font-bold text-[#47685b]"><span>الوصول</span><select value={visibility} onChange={event => setVisibility(event.target.value as Visibility)} className="h-11 w-full rounded-xl border border-[#dce5da] bg-white px-3 text-right"><option value="public">عام</option><option value="members">للأعضاء</option></select></label></div><Button disabled={create.isPending} className="w-full rounded-xl bg-[#0d4937] text-xs hover:bg-[#176047]">{create.isPending && <Loader2 className="animate-spin" size={15} />}{parentId ? "إنشاء المجتمع الفرعي" : "إنشاء المساحة"}</Button></form>;
}

export function CommunitiesPage() {
  const [, navigate] = useLocation();
  const { isAuthenticated } = useAuth();
  const communities = trpc.social.communities.useQuery();
  const [showCreate, setShowCreate] = useState(false);
  return <PlatformShell><main className="mx-auto w-full max-w-[1240px] px-4 py-7 sm:px-6 lg:px-8 lg:py-10"><section className="overflow-hidden rounded-[28px] border border-[#cfe0d2] bg-[#0d4937] px-6 py-7 text-white shadow-[0_20px_50px_rgba(13,59,49,0.15)] sm:px-8"><p className="text-xs font-extrabold tracking-[0.18em] text-[#d9b85b]">مجتمعات دائرة الأمة</p><div className="mt-4 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div className="max-w-2xl"><h1 className="font-display text-3xl font-semibold tracking-[-0.045em] sm:text-4xl">مكان للصحبة النافعة، لا للضجيج.</h1><p className="mt-3 max-w-xl text-sm leading-7 text-[#d6e6dc]">أنشئ مجتمعًا أو مجموعة بغاية واضحة، ثم افتح مجتمعًا فرعيًا عند الحاجة. لا اقتراحات قهرية ولا تمرير بلا نهاية.</p></div><Button onClick={() => isAuthenticated ? setShowCreate(value => !value) : navigate("/auth")} className="shrink-0 rounded-xl bg-[#d9b85b] text-xs font-extrabold text-[#173e31] hover:bg-[#ead178]"><Plus size={16} />أنشئ مساحة</Button></div></section><div className="mt-6 grid gap-6 lg:grid-cols-[minmax(0,1fr)_340px]"><section><div className="mb-4 flex items-center justify-between"><div><h2 className="font-display text-2xl font-semibold text-[#244c3e]">المجتمعات المتاحة</h2><p className="mt-1 text-sm text-[#738a7f]">اختر مساحة توافق هدفك، ثم انضم بإرادتك.</p></div><span className="rounded-full bg-[#e5efe5] px-3 py-1.5 text-xs font-extrabold text-[#176047]">{communities.data?.length ?? 0} مساحة</span></div>{communities.isLoading ? <div className="flex min-h-52 items-center justify-center rounded-[24px] border border-[#dce5da] bg-white text-sm text-[#668075]"><Loader2 className="ml-2 animate-spin" size={17} />يجري تحميل المساحات…</div> : communities.data?.length ? <div className="grid gap-4 sm:grid-cols-2">{communities.data.map(community => <button key={community.id} onClick={() => navigate(`/communities/${community.slug}`)} className="group rounded-[24px] border border-[#dce5da] bg-white p-5 text-right shadow-[0_12px_30px_rgba(16,61,46,0.04)] transition hover:-translate-y-0.5 hover:border-[#aac5ad]"><div className="flex items-start justify-between gap-3"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-[#e6f0e5] text-[#176047]"><UsersRound size={20} /></span><CommunityBadge kind={community.kind} visibility={community.visibility} /></div><h3 className="mt-5 font-display text-xl font-semibold text-[#244c3e] group-hover:text-[#0d4937]">{community.name}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-[#6b8377]">{community.description}</p><div className="mt-5 flex items-center justify-between border-t border-[#edf2eb] pt-3 text-xs font-bold text-[#799084]"><span>{community.memberCount} عضو</span><span className="text-[#176047]">{community.joined ? "أنت عضو" : "افتح المساحة"}</span></div></button>)}</div> : <div className="rounded-[24px] border border-dashed border-[#c8d8c9] bg-[#fbfcf8] px-6 py-14 text-center"><Layers3 className="mx-auto text-[#6e927d]" size={28} /><h3 className="mt-4 font-display text-xl font-semibold text-[#244c3e]">ابدأ بأول مساحة نافعة.</h3><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-[#71887c]">أنشئ مجتمعًا بهدف واضح، مثل التعلّم أو خدمة الناس أو تدبّر القرآن.</p></div>}</section><aside className="space-y-4">{showCreate && <CreateCommunityForm onDone={newSlug => navigate(`/communities/${newSlug}`)} />}<div className="rounded-[24px] border border-[#e2d6ad] bg-[#faf5e5] p-5"><h3 className="font-display text-lg font-semibold text-[#5c522f]">قاعدة المساحة</h3><p className="mt-2 text-sm leading-6 text-[#746b4c]">لا تنشئ مساحة بلا هدف. اكتب فائدة واضحة، واحفظ أدب الحوار، ولا تستخدم الاسم أو التصميم لخداع الناس.</p></div></aside></div></main></PlatformShell>;
}

export function CommunityDetailPage() {
  const { slug } = useParams<{ slug: string }>();
  const [, navigate] = useLocation();
  const { user, isAuthenticated } = useAuth();
  const utils = trpc.useUtils();
  const community = trpc.social.community.useQuery({ slug: slug ?? "" });
  const communityId = community.data?.community.id ?? 0;
  const feed = trpc.social.communityFeed.useQuery({ communityId }, { enabled: Boolean(communityId) });
  const [content, setContent] = useState("");
  const [attachments, setAttachments] = useState<Attachment[]>([]);
  const [showSubcommunityForm, setShowSubcommunityForm] = useState(false);
  const [reportPost, setReportPost] = useState<CommunityPost | null>(null);
  const [reportCategory, setReportCategory] = useState<ReportCategory>("scam");
  const [reportDetails, setReportDetails] = useState("");
  const [editingPost, setEditingPost] = useState<CommunityPost | null>(null);
  const [editedContent, setEditedContent] = useState("");
  const fileInputRef = useRef<HTMLInputElement>(null);
  const join = trpc.social.joinCommunity.useMutation({ onSuccess: () => { toast.success("أصبحت عضوًا في هذه المساحة."); void utils.social.community.invalidate({ slug: slug ?? "" }); void utils.social.communities.invalidate(); }, onError: error => toast.error(error.message) });
  const leave = trpc.social.leaveCommunity.useMutation({ onSuccess: () => { toast.success("غادرت المساحة."); void utils.social.community.invalidate({ slug: slug ?? "" }); void utils.social.communities.invalidate(); }, onError: error => toast.error(error.message) });
  const createPost = trpc.social.createPost.useMutation({ onSuccess: result => { setContent(""); setAttachments([]); void feed.refetch(); if (result.moderation.status === "under_review") toast.message(result.moderation.message, { duration: 10_000, action: { label: "مراسلة المالك", onClick: () => { window.location.href = ownerReviewMailto(result.postId); } } }); else toast.success("تم نشر مشاركتك في المساحة."); }, onError: error => toast.error(error.message) });
  const upload = trpc.social.uploadAttachment.useMutation();
  const deletePost = trpc.social.deletePost.useMutation({ onSuccess: () => { void feed.refetch(); toast.success("تم حذف منشورك."); }, onError: error => toast.error(error.message) });
  const updatePost = trpc.social.updatePost.useMutation({ onError: error => toast.error(error.message) });
  const submitReport = trpc.social.submitReport.useMutation({ onSuccess: () => { setReportPost(null); setReportDetails(""); setReportCategory("scam"); toast.success("أُرسل البلاغ إلى مالك المنصة. شكرًا لحرصك."); }, onError: error => toast.error(error.message) });
  const details = community.data;
  if (community.isLoading) return <PlatformShell><main className="mx-auto flex min-h-[55vh] max-w-4xl items-center justify-center px-4 text-sm text-[#668075]"><Loader2 className="ml-2 animate-spin" size={18} />يجري فتح المساحة…</main></PlatformShell>;
  if (community.error || !details) return <PlatformShell><main className="mx-auto max-w-4xl px-4 py-12"><div className="rounded-[24px] border border-[#e2d6ad] bg-[#faf5e5] p-6 text-center"><h1 className="font-display text-2xl font-semibold text-[#5c522f]">تعذّر فتح المساحة.</h1><p className="mt-2 text-sm text-[#746b4c]">قد تكون المساحة للأعضاء فقط أو لم تعد موجودة.</p><Button onClick={() => navigate("/communities")} className="mt-5 rounded-xl bg-[#0d4937] text-xs">العودة إلى المجتمعات</Button></div></main></PlatformShell>;
  const { community: item, membership } = details;
  const ensureSignedIn = () => { if (isAuthenticated) return true; toast.info("سجّل الدخول للمشاركة في الدائرة."); navigate("/auth"); return false; };
  const joinOrLeave = () => { if (!ensureSignedIn()) return; if (membership?.role === "owner") return toast.message("أنت مالك هذه المساحة."); if (membership) return leave.mutate({ communityId: item.id }); join.mutate({ communityId: item.id }); };
  const onFileSelect = async (event: React.ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; event.target.value = ""; if (!file || !ensureSignedIn()) return; if (attachments.length >= 5) return toast.error("يمكنك إرفاق خمسة عناصر كحد أقصى في المنشور الواحد."); if (file.size > 50_000_000) return toast.error("حجم المرفق يجب أن يكون 50 ميغابايت أو أقل."); const base64 = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result).split(",")[1] || ""); reader.onerror = () => reject(new Error("تعذّرت قراءة الملف.")); reader.readAsDataURL(file); }); try { const attachment = await upload.mutateAsync({ filename: file.name, mimeType: file.type || "application/octet-stream", dataBase64: base64 }); setAttachments(current => [...current, attachment]); toast.success(`أُرفق ${file.name}.`); } catch (error) { toast.error(error instanceof Error && error.message ? error.message : "تعذّر رفع المرفق. تحقّق من اتصالك ثم حاول مجددًا."); } };
  const publish = (event: React.FormEvent) => { event.preventDefault(); if (!content.trim()) return; createPost.mutate({ content: content.trim(), visibility: "public", communityId: item.id, attachments: attachments.map(attachment => ({ kind: attachment.kind, url: attachment.url, storageKey: attachment.storageKey ?? null, filename: attachment.filename ?? null, mimeType: attachment.mimeType ?? null, sizeBytes: attachment.sizeBytes ?? null })) }); };
  const openReport = (post: CommunityPost) => { if (!ensureSignedIn()) return; setReportPost(post); setReportCategory("scam"); setReportDetails(""); };
  const sendReport = () => { if (reportPost) submitReport.mutate({ postId: reportPost.id, category: reportCategory, ...(reportDetails.trim() ? { details: reportDetails.trim() } : {}) }); };
  const deleteOwnPost = (postId: number) => { if (window.confirm("هل تريد حذف منشورك نهائيًا؟ لا يمكن التراجع عن هذا الإجراء.")) deletePost.mutate({ postId }); };
  const saveEdit = () => { if (!editingPost || !editedContent.trim()) return toast.error("اكتب محتوى المنشور أولًا."); updatePost.mutate({ postId: editingPost.id, content: editedContent.trim() }, { onSuccess: () => { void feed.refetch(); setEditingPost(null); toast.success("تم تحديث منشورك."); } }); };
  const setPostVisibility = (postId: number, visibility: "public" | "friends") => updatePost.mutate({ postId, visibility }, { onSuccess: () => { void feed.refetch(); toast.success(visibility === "public" ? "أصبح المنشور ظاهرًا للعامة." : "أصبح المنشور للأصدقاء فقط."); } });
  const posts = (feed.data ?? []) as CommunityPost[];
  return <PlatformShell><main className="mx-auto w-full max-w-[1120px] px-4 py-7 sm:px-6 lg:px-8 lg:py-10"><button onClick={() => navigate("/communities")} className="mb-5 text-xs font-extrabold text-[#356953] hover:underline">← العودة إلى المجتمعات</button><section className="rounded-[28px] border border-[#cfe0d2] bg-white p-6 shadow-[0_14px_38px_rgba(16,61,46,0.05)] sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div className="max-w-2xl"><CommunityBadge kind={item.kind} visibility={item.visibility} /><h1 className="mt-4 font-display text-3xl font-semibold tracking-[-0.045em] text-[#1e493a] sm:text-4xl">{item.name}</h1><p className="mt-3 text-sm leading-7 text-[#668075]">{item.description}</p><p className="mt-4 text-xs font-bold text-[#81978b]">{details.memberCount} عضوًا في هذه المساحة</p></div><Button onClick={joinOrLeave} disabled={join.isPending || leave.isPending} className="rounded-xl bg-[#0d4937] text-xs hover:bg-[#176047]">{membership?.role === "owner" ? "أنت المالك" : membership ? "مغادرة المساحة" : "انضم إلى المساحة"}</Button></div></section>{membership && <section className="mt-6 rounded-[24px] border border-[#dce5da] bg-white p-5"><div className="mb-3 flex items-center justify-between"><h2 className="font-display text-lg font-semibold text-[#244c3e]">شارك بفائدة</h2><EmojiPicker onSelect={emoji => setContent(value => `${value}${value ? " " : ""}${emoji}`)} disabled={upload.isPending} /></div><form onSubmit={publish}><Textarea value={content} onChange={event => setContent(event.target.value)} maxLength={5000} placeholder={`ما الذي تريد مشاركته في ${item.name}؟`} className="min-h-28 rounded-xl border-[#dce5da] text-right" />{attachments.length > 0 && <div className="mt-3 flex flex-wrap gap-2">{attachments.map((attachment, index) => <span key={`${attachment.url}-${index}`} className="inline-flex items-center gap-1.5 rounded-lg bg-[#edf4ee] py-1.5 pl-2.5 pr-1 text-[11px] font-bold text-[#416d5b]">{attachment.kind === "image" || attachment.kind === "gif" ? <ImagePlus size={13} /> : attachment.kind === "video" ? <Video size={13} /> : <FileText size={13} />}<span className="max-w-32 truncate">{attachment.kind === "gif" ? "GIF" : attachment.filename || attachment.kind}</span><button type="button" onClick={() => setAttachments(current => current.filter((_, itemIndex) => itemIndex !== index))} className="rounded-md px-1 py-0.5 text-[#6d8f7d] hover:bg-white hover:text-[#a14f36]" aria-label="حذف المرفق">×</button></span>)}</div>}<div className="mt-3 flex items-center justify-between border-t border-[#edf2eb] pt-3"><div className="flex items-center gap-2"><input ref={fileInputRef} type="file" className="hidden" accept="image/*,image/gif,video/*,.pdf,.txt,.doc,.docx,.xls,.xlsx" onChange={onFileSelect} /><button type="button" onClick={() => fileInputRef.current?.click()} disabled={upload.isPending} className="grid h-9 w-9 place-items-center rounded-xl text-[#467566] transition-colors hover:bg-[#e8f1e7] hover:text-[#176047]" aria-label="إرفاق صورة أو GIF أو فيديو أو ملف">{upload.isPending ? <Loader2 className="animate-spin" size={18} /> : <Upload size={18} />}</button><span className="text-[11px] text-[#81978b]">منشورات هذه المساحة محدودة وليست تمريرًا لا نهائيًا.</span></div><Button disabled={createPost.isPending || upload.isPending || !content.trim()} className="rounded-xl bg-[#0d4937] text-xs hover:bg-[#176047]">{createPost.isPending && <Loader2 className="animate-spin" size={14} />}انشر بأدب</Button></div></form></section>}<section className="mt-6"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-display text-2xl font-semibold text-[#244c3e]">منشورات المساحة</h2><p className="mt-1 text-sm text-[#738a7f]">خلاصة محدودة: حتى 40 منشورًا حديثًا.</p></div><MessageSquareText className="text-[#6e927d]" size={22} /></div>{feed.isLoading ? <div className="rounded-[24px] border border-[#dce5da] bg-white p-10 text-center text-sm text-[#668075]"><Loader2 className="mx-auto mb-2 animate-spin" size={18} />يجري تحميل المنشورات…</div> : posts.length ? <div className="space-y-4">{posts.map(post => <CommunityPostCard key={post.id} post={post} isAuthor={post.author.id === user?.id} onReport={() => openReport(post)} onDelete={() => deleteOwnPost(post.id)} onEdit={() => { setEditingPost(post); setEditedContent(post.content); }} onSetVisibility={visibility => setPostVisibility(post.id, visibility)} />)}</div> : <div className="rounded-[24px] border border-dashed border-[#c8d8c9] bg-[#fbfcf8] px-6 py-12 text-center"><MessageSquareText className="mx-auto text-[#6e927d]" size={27} /><h3 className="mt-4 font-display text-xl font-semibold text-[#244c3e]">لا توجد منشورات بعد.</h3><p className="mt-2 text-sm text-[#71887c]">ابدأ بمشاركة نافعة، أو انضم أولًا لتتمكن من النشر.</p></div>}</section>{membership && <section className="mt-6"><div className="flex items-center justify-between"><div><h2 className="font-display text-2xl font-semibold text-[#244c3e]">المجتمعات الفرعية</h2><p className="mt-1 text-sm text-[#738a7f]">افتح موضوعًا أدق داخل هذه المساحة عند الحاجة.</p></div><Button onClick={() => setShowSubcommunityForm(value => !value)} variant="outline" className="rounded-xl border-[#c9d9c8] bg-white text-xs text-[#176047]"><Plus size={15} />إضافة فرعي</Button></div>{showSubcommunityForm && <div className="mt-4 max-w-xl"><CreateCommunityForm parentId={item.id} onDone={newSlug => navigate(`/communities/${newSlug}`)} /></div>}{details.subcommunities.length ? <div className="mt-4 grid gap-3 sm:grid-cols-2">{details.subcommunities.map(subcommunity => <button key={subcommunity.id} onClick={() => navigate(`/communities/${subcommunity.slug}`)} className="rounded-2xl border border-[#dce5da] bg-white p-4 text-right hover:border-[#aac5ad]"><CommunityBadge kind={subcommunity.kind} visibility={subcommunity.visibility} /><h3 className="mt-3 font-bold text-[#244c3e]">{subcommunity.name}</h3><p className="mt-1 line-clamp-2 text-xs leading-5 text-[#71887c]">{subcommunity.description}</p></button>)}</div> : <p className="mt-4 rounded-2xl bg-[#f6f8f4] p-4 text-sm text-[#71887c]">لا توجد مجتمعات فرعية بعد.</p>}</section>}<Dialog open={!!reportPost} onOpenChange={open => { if (!open) setReportPost(null); }}><DialogContent dir="rtl" className="max-w-md rounded-[24px] border-[#dbe5dc] bg-[#fbfcf8] p-6"><DialogHeader><span className="mb-2 grid h-11 w-11 place-items-center rounded-2xl bg-[#f8ece6] text-[#a14f36]"><Flag size={21} /></span><DialogTitle className="font-display text-2xl tracking-[-0.035em] text-[#174035]">الإبلاغ عن منشور</DialogTitle><DialogDescription className="pt-1 leading-6 text-[#688075]">يُسجَّل البلاغ ويُرسل من خادم دائرة الأمة إلى مالك المنصة. لا يُفتح بريدك الشخصي.</DialogDescription></DialogHeader><div className="grid grid-cols-2 gap-2">{([ ["scam", "احتيال"], ["lie", "كذب"], ["brainrot", "محتوى مُفسد للعقل"], ["haram imagery", "صور محرَّمة"] ] as const).map(([value, label]) => <button key={value} type="button" onClick={() => setReportCategory(value)} className={`rounded-xl border px-3 py-2 text-right text-xs font-bold transition-colors ${reportCategory === value ? "border-[#b6794d] bg-[#fbf0e9] text-[#914a31]" : "border-[#dce6dc] bg-white text-[#547468] hover:border-[#bdcfbf]"}`}>{label}</button>)}</div><Textarea value={reportDetails} onChange={event => setReportDetails(event.target.value)} placeholder="أضف تفاصيل مختصرة إن وُجدت (اختياري)…" className="min-h-28 rounded-xl border-[#dce6dc]" maxLength={2000} /><DialogFooter><Button variant="outline" onClick={() => setReportPost(null)} className="rounded-xl">إلغاء</Button><Button onClick={sendReport} disabled={submitReport.isPending} className="rounded-xl bg-[#a14f36] hover:bg-[#8d422c]">{submitReport.isPending ? <Loader2 className="animate-spin" size={16} /> : <Flag size={16} />}إرسال البلاغ</Button></DialogFooter></DialogContent></Dialog><Dialog open={!!editingPost} onOpenChange={open => { if (!open) setEditingPost(null); }}><DialogContent dir="rtl" className="max-w-md rounded-[24px] border-[#dbe5dc] bg-[#fbfcf8] p-6"><DialogHeader><DialogTitle className="font-display text-2xl tracking-[-0.035em] text-[#174035]">تحرير المنشور</DialogTitle><DialogDescription className="pt-1 leading-6 text-[#688075]">عدّل كلامك مع الحفاظ على قواعد الدائرة.</DialogDescription></DialogHeader><Textarea value={editedContent} onChange={event => setEditedContent(event.target.value)} className="min-h-36 rounded-xl border-[#dce6dc]" maxLength={5000} /><DialogFooter><Button variant="outline" onClick={() => setEditingPost(null)} className="rounded-xl">إلغاء</Button><Button onClick={saveEdit} disabled={updatePost.isPending || !editedContent.trim()} className="rounded-xl bg-[#0d4937] hover:bg-[#176047]">حفظ التعديل</Button></DialogFooter></DialogContent></Dialog></main></PlatformShell>;
}
